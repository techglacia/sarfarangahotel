/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.ibb.co'],  // Add 'i.ibb.co' to the list of allowed domains for external images
    },
  };
  
  export default nextConfig;
  